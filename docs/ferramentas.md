# Ferramentas

Tribunal TJPA: Certidão Criminal expõe 1 ferramenta (todas somente leitura).

### 1. `tribunal_tjpa_cert_criminal_consultar`
**Input**: `nome_requerente`, `nome_mae`, `endereco`, `rg` (opcional), `cpf` (opcional), `orgao_emissor_rg` (opcional), `uf_emissor_rg` (opcional)

Tribunal TJPA: Certidão Criminal, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Tribunal TJPA: Certidão Criminal
```
